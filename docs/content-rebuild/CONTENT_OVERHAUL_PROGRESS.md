# GoArgentina — Content Overhaul Progress

Working log for the master content-overhaul task ("не заканчивай работу на аудите"). This
file is updated as work continues; it is not a one-time audit report.

Branch: `cursor/content-overhaul-e4a2`
Base: `main` @ `81055b13`

## 0. Canonical-source discovery (required before touching anything)

Before editing, mapped what is actually live vs. staged vs. legacy, per the task's
`CURRENT_CANONICAL / CURRENT_RUNTIME / LEGACY / DRAFT / GENERATED` framework:

| Layer | Location | Status |
| --- | --- | --- |
| Knowledge base (source of truth for `/baza-znaniy/*`) | `content/knowledge-base/**/*.md` + `content/knowledge-base/_index/build_manifest.py` → `content.json` | **CURRENT_RUNTIME.** `src/lib/knowledge-base/content.ts` reads `content.json` directly. A strict publication gate (`src/lib/knowledge-base/publication-quality.ts`) decides which of the 691 entities are actually public (`isPublicKbEntry`); the rest are quarantined, not deleted. Confirmed live: 691 raw → 237 public after this session's fixes (see §6 for the full trail). |
| Blog | `src/data/blog-manual-from-md/*.ts` (generated from `docs/articles/*.md` via `npm run sync-manual-posts`), `src/data/blog-*.ts`, CMS documents under `admin/content/documents` | **CURRENT_RUNTIME**, mixed static/CMS. 290 posts scanned by `scripts/editorial-audit.ts`. |
| Guide pillars, immigration hub, destinations | `src/data/guide-pillars/*.ts`, `src/data/immigration-*.ts`, `src/data/destination-pages.ts` | **CURRENT_RUNTIME**, static TS, not CMS-backed. |
| Prior editorial program (this project's own "B00x") | `docs/content-overhaul/**` (35 CSV/MD artifacts: inventory, source registry, claim registry, issue ledger, media audit, taxonomy, style guide, etc.) generated by `scripts/content-overhaul-inventory.ts` | **Editorial legacy, still authoritative process.** Built by a previous session (`git log` → commit `e9a4ae02`, "feat: overhaul knowledge base publication system"), last regenerated 2026-07-20. This is exactly the kind of prior work the task told us not to ignore or blindly overwrite — it already delivered most of what §22 of the task brief asks for (route inventory, claim registry, source registry, media rights register, orphan/duplicate/thin-content reports, redirect map, editorial style guide, widget registry). Re-used it as the primary discovery tool instead of re-inventing it. |
| `content/knowledge-base/README.md` claim "site sync not started" | — | **Outdated note, superseded.** The KB *is* wired to the live `/baza-znaniy/*` route today; the README predates the publication-quality gate. Left as-is (out of scope to rewrite internal docs this pass) but flagged here so the next agent doesn't repeat the same false assumption. |
| Old Master Goal V6 sprint program (Sprints 1–6, `PROJECT_STATE.md`) | `docs/project-governance/*` | Separate, still-open infra/reliability program (Supabase egress quota, Vercel `Account is blocked`, payment/booking hardening). Not this task's scope; did not touch. |

## 1. Tooling already in the repo (used, not re-built)

The repo already ships a large content-audit toolchain (`npm run content:*`, `kb:*`,
`editorial:*`, `guide:editorial-readiness`, `blog:editorial-readiness`, `media:*`). Ran the
full set at session start as the discovery mechanism instead of manually re-deriving route
inventories, broken-link crawls, etc. Key baseline results (before any edits):

- `content:audit` / `content:governance` / `content:links --strict`: **0 broken public
  links** out of 910 unique external URLs cited by the KB (`brokenPublic: 0`), 0 unresolved
  internal links, 0 structural errors.
- `editorial-audit.ts` (blog design-system audit): 290 posts, 0 errors, **24 warnings** —
  investigated below (§2).
- `guide-editorial-readiness` / `blog-editorial-readiness`: 29/29 and 67/67 passed.
- `kb:source-health` (live HTTP check of every cited external source): 301/325 passed, 24
  failed. Investigated below (§3) — most were sandbox/WAF false positives; 2 were genuinely
  dead links.
- `docs/content-overhaul/{thin,duplicate,orphan,broken-links}-report.csv`: all empty/closed
  as of 2026-07-20 (previous session already resolved these categories).

## 2. Fixed: stale widget allowlist causing false "unknown widget" warnings

**Root cause.** `src/editorial/utilities/audit.ts` hard-coded a `KNOWN_WIDGETS` set with only
the original 3 widget keys (`season-matrix`, `tourism-infographic`, `tourism-timeline`).
Fifteen real, rendered widgets added later (`steak-cut-selector`, `steak-cut-diagram`,
`steak-doneness-phrases`, `steak-order-scenarios`, `steak-bill-explainer`, `tango-glossary`,
`tango-ronda-diagram`, `tango-phrasebook`, `trip-type-selector`, `layer-system`,
`packing-list`, `destination-packing-cards`, `summer-winter-comparison`,
`what-not-to-pack`, `carry-on-packing`) were never added to this list, so every article using
them (`argentinian-steak-guide`, `tango-beginners-guide`, `patagonia-packing-list`) was
flagged as referencing an "unknown widget" — even though
`src/components/travel/TravelWidgetRenderer.tsx` renders every one of them correctly in
production.

**Fix.** `TravelWidgetRenderer.tsx` now exports `TRAVEL_WIDGET_KEYS` (the actual
canonical list, derived from its own `TravelWidgetKey` union) and the editorial audit imports
it instead of maintaining a second, drifting copy. Editorial audit warnings dropped from
**24 → 9** (the remaining 9 are legitimate "long text without a visual block" /
"too many callouts" editorial suggestions, plus informational `legacy-slug` notices for
intentionally-noindexed programmatic pages — not defects).

**Separately verified via the pre-existing issue ledger**
(`docs/content-overhaul/issue-ledger.csv`, row `widgets:unknown-editorial-key`): the specific
concern that an *unknown* widget key would leak a raw `widget: {key}` string to real users is
already **not reproducible** in the current code — `TravelWidgetRenderer` returns `null` for
any unrecognized key, and `BlogWidgetBlock`/`BlogSectionBody` don't add a fallback string
either. This looks like it was already fixed in a later commit than the one that produced
that ledger row; no code change was needed for that specific finding, only for the stale
allowlist above.

Commit: `fix(content): derive editorial widget allowlist from TravelWidgetRenderer`.
Verified: `npx tsc --noEmit`, `eslint` on both files, `vitest run src/editorial src/components/travel …` (92 tests), full `audit:quick`.

## 3. Fixed: dead/superseded official sources on live `/baza-znaniy/*` pages (P0)

Per the task's fact-checking priority (legal/migration/financial claims first), verified
every source flagged by `kb:source-health` against the live internet (not just the sandbox
fetch used by the repo's own health-check script, which is blocked by several government WAFs
and produced some false positives). Genuine findings and fixes:

1. **`grazhdanstvo`, `oshibka-s-dokumentami`, `nuzhno-li-otkazyvatsya-ot-grazhdanstva`,
   `kak-poluchit-grazhdanstvo-za-2-goda`** (all live at `/baza-znaniy/<id>`): the general
   `argentina.gob.ar/servicio/obtener-la-ciudadania-argentina` page — cited as the primary
   source for "citizenship is handled by the courts" — now returns "página no disponible".
   Confirmed via live fetch. Consistent with DNM having moved citizenship-by-naturalization
   fully digital via RaDEX in October 2025 (independently confirmed: DNM press release,
   `argentina.gob.ar/migraciones/ciudadania-autogestion`, `migraciones.gob.ar/radex/...`).
   Removed the dead source from `sources:` entirely (a source you can't verify can't be a
   "primary" source, regardless of expiry dates) instead of leaving it listed as
   `verified`; rewrote the affected passages and the "double citizenship / no renunciation"
   claim to cite the actual **Ley 346** naturalization text (verified live — Art. 2 lists only
   the 2-year-residency and investment routes, no renunciation requirement) instead of the
   dead general page.
2. **`gid-relokanta`, `gid-po-dengam`**: BCRA's migrant-account page moved from
   `/noticias/cuenta-gratuita-inmigrantes.asp` (404, confirmed) to
   `/noticias/cuenta-bancaria-para-inmigrantes/` (confirmed live, same content). Updated both
   references.
3. **`stoimost-zhizni-ba`**: the cited INDEC IPC figure (May 2026: 2.1% MoM / 33.2% YoY) had
   been superseded by the July 2026 release (published 2026-08-13: 2.1% MoM / 19.3% YTD /
   **33.8%** YoY — inflation re-accelerated). Replaced the citation and the numbers in the
   body text.
4. Re-verified as still live (no change needed beyond refreshing `checked_at`): DNU 366/2025
   text, Ley 25.871 text, DNM tourist-entry-documents page, DNM digital-nomad-residence page,
   the Argentina–Russia visa-waiver treaty PDF (fetched and diffed against the article's
   claim — Art. 1 does say 90/180 days from first entry, matches exactly), BCRA
   FX-liberalization notices, BCRA CBU/CVU educational PDF, Western Union's Argentina
   transfer page, ARCA's live monotributo category table (confirmed it now carries an August
   2026 effective date, not February as one file's stale note claimed — fixed), IGJ's SAS and
   urgent-registration pages, `argentina.gob.ar/tema/emergencias`.
5. Confirmed **not actually broken**, just blocked by anti-bot protection when fetched from
   this sandbox: `flybondi.com/ar` (Cloudflare challenge), `bna.com.ar/Personas` (WAF
   "Request Rejected"), several `anses.gob.ar` and `argentina.gob.ar` service pages (403 to
   the health-check script's user agent, 200 with real content to a normal fetch). Marked the
   BNA source `unchecked` (not falsely `verified`) with a note that it needs a human/browser
   recheck rather than an automated one.

**Side effect discovered and fixed**: regenerating `content/knowledge-base/_index/` with
today's real date (vs. the git-committed snapshot from 2026-08-17) mechanically flipped 34
previously-public KB entries to `quarantined`, because their 30/45-day source-recheck window
had elapsed in the intervening days — a real, pre-existing freshness backlog, not something
the edits above caused. This broke two vitest contracts
(`content-overhaul-inventory.test.ts`, `public-link-contract.test.ts`) that pin the public KB
count and check that no hardcoded internal link points at a now-quarantined entry. Closed
both properly: fixed 10 of the affected entries for real (all listed above), regenerated
`docs/content-overhaul/inventory-summary.json` and related CSVs from the corrected corpus
instead of hand-editing the numbers, and confirmed `requiredTopicGaps` is back to `0/38` (the
"Гражданство" / "Банки, карты, переводы" / "Работа и бизнес" topic clusters each have a
strict-ready canonical article again). Full `npm run audit:quick` — 488 files / 2391 tests,
tsc, lint — is green on the final state.

Commit: `content: fact-check knowledge base sources (citizenship, banking, visa, tax)` and
`content: close KB publication-gate regressions from freshness sweep`.

## 4. KB P0 backlog — closed out (session 2)

Continued the exact §3 process (live fetch of every cited source; remove or replace what's
genuinely dead; mark `unchecked` with a note instead of blind-bumping what the sandbox's fetch
tool couldn't reach) across the entire remaining 24-entry backlog from §3. Result: **24 → 6**.
Each of the 18 newly-closed entries got its sources independently re-fetched and its
`checked_at`/`verified_at`/`last_verified` fields updated only where a source was actually
confirmed live this session (commits `content: continue KB fact-check sweep…` and
`content: complete first pass of KB fact-check backlog…`):

- **Safety/health**: `bezopasno-li-v-argentine`, `bezopasno-li-odnoj-zhenshchine` — re-verified
  UK FCDO, Government of Canada, APN park-safety and Línea-144 pages.
  `medicina-i-strahovka`, `gid-po-medicine` — found and documented a **real discrepancy between
  two official Argentine government pages** about which provinces the 107 medical-emergency
  number covers (`argentina.gob.ar/tema/emergencias` says CABA + part of Buenos Aires province
  only; the Health Ministry's own street-injury table says 107 covers almost every other
  province, with 911 specific to Buenos Aires province and 106 to Neuquén). Rewrote both
  articles to state the conflict honestly and advise calling 911 in any case, rather than
  picking one official source as authoritative without evidence. `skam-s-falshivoj-policiej`,
  `kakie-rajony-ba-izbegat` — re-verified the 911-guidance, CABA tourist-security and
  crime-map-office pages.
- **Money**: `chto-takoe-mep`, `byudzhet-poezdki` — re-verified CNV investor-protection pages,
  APN's June-2026 park-tariff table, SUBE's August-2026 AMBA fare table, and the Bank of
  Russia's 2022 cross-border card notice. `kak-platit-esli-karta-rf-ne-rabotaet`,
  `kak-oplatit-tur-i-zhilyo-zaranee` — the cited Mastercard press release
  (`mastercard.com/au/en/...`) returns a 403 from this environment; replaced it with the same
  statement's permanent SEC EDGAR filing copy (independently reachable, byte-for-byte identical
  text, cross-checked against `investor.mastercard.com` and `businesswire.com`).
  `udalyonnaya-rabota-i-oplata`, `stoimost-zhizni-semya-i-para`,
  `zashchita-nakoplenij-ot-inflyacii` — re-verified ARCA service-export rules, Wise's Russia
  availability page, BCRA's UVA-deposit note, CNV's virtual-assets warning and CNV's idóneos
  register.
- **Immigration**: `rabota-i-poisk-raboty` — re-verified the foreign-worker guidance page, the
  trabajador-migrante residence page, ANSES's CUIL procedure and the RIPTE wage index (June 2026
  release). `skolko-stoit-oformit-vnzh` — re-verified DNM's live migratory-fee table
  (Decreto 584/2024, values in UMSM with the current unit value stated on the page itself) and
  the residencias FAQ. `viza-cifrovogo-kochevnika-chto-daet` — re-verified the DNM digital-nomad
  page and Disposición 758/2022. `viza-v-braziliyu-dlya-iguasu` — fetched Brazil's Decreto
  7271/2010 text directly (Art. 1: 90/180-day rule, excludes paid work/study/internships/
  volunteering) instead of trusting a stale citation.
- **Property**: `pokupka-nedvizhimosti` — re-verified Constitución art. 20, Ley 26.737, the
  tierras-rurales certificate page, ARCA's CDI page and Colegio de Escribanos' compraventa FAQ.
- **Cost of living**: `pereezd-s-minimalnym-byudzhetom` — replaced the May-2026 INDEC canasta
  básica figure with the July-2026 release (published 2026-08-13: CBA 708,016 ARS / CBT
  1,564,716 ARS for a family of four).

### The 6 that remain, and exactly why

Every one of these is blocked by the *same class* of problem — a source that is very likely
still fine but that this session's sandboxed fetch tool could not honestly confirm — not a
contested or dangerous fact:

| Entry | Blocking source | Reason |
| --- | --- | --- |
| `aktualen-li-blue-dollar` | `bna.com.ar/Personas` | Site returns "Request Rejected" (WAF) to the automated fetch tool. |
| `skolko-deneg-brat-na-poezdku` | `bna.com.ar/Personas` | Same WAF. |
| `skolko-stoit-zhizn-v-buenos-ajrese` | `bna.com.ar/Personas` | Same WAF. |
| `viza-v-braziliyu-dlya-iguasu` | `gov.br/mre/.../vivis-visit-visa/...` | Fetch timed out twice (Brazil's MRE consular site); the core fact (90/180 days) is independently confirmed via the decree text itself, so this is now a secondary/corroborating source only. |
| `zashchita-nakoplenij-ot-inflyacii` | `cnv.gov.ar/.../TOC2013.pdf` | CNV's full "Texto Ordenado" is a very large PDF; timed out twice. |
| `pokupka-nedvizhimosti` | Código Civil y Comercial full text (`ley-26994-235975`) + AGIP `beneficios/64` | The Código Civil is a huge document that timed out twice; AGIP's stamp-tax page also timed out, but its 2026 threshold ($205,332,000) is independently corroborated by the (successfully fetched) Colegio de Escribanos compraventa FAQ. |

**Next action for a future session:** re-fetch these 6 specific URLs with a real browser (not
this sandbox's fetch tool) or from a different network path, then flip `url_status` to
`verified` with today's date — do **not** guess based on how old they look.

## 4b. Media, blog, guide-pillar and system-content sweep (session 2)

Beyond the KB, went through blog, guide pillars, immigration hub data, and system copy (FAQ,
legal, testimonials, organizer identities) looking for the same classes of problem the KB pass
found (stale volatile figures, dead sources, contradictions between pages, fabricated trust
claims). Real findings and fixes (commits `content: fix stale SMVM figure…`,
`content: refresh verification dates for guide pillars…`,
`content: fix stale blue-dollar advice in site FAQ…`):

- **`src/data/blog-manual-from-md/vnzh-argentina-rezidenciya.ts`** — this is a live, *featured*
  blog post (`/blog/vnzh-argentina-rezidenciya`). It hardcoded the rentista income threshold as
  "5× SMVM ≈ 363,000 ARS (May 2026)". SMVM (Argentina's minimum wage) is adjusted almost every
  month; a live search confirmed the real August-2026 value is 376,600 ARS (Resolución
  9/2025) — about 3.7% higher. Replaced the frozen number and told the reader explicitly to
  re-check DNM's page for the value on their actual filing date, instead of leaving either
  number looking authoritative.
- **`src/data/faq.ts`** — the site FAQ's "card or cash" answer still told everyone to
  prioritize street/"blue dollar" exchange as the default strategy, contradicting the more
  accurate guidance already fixed in the KB this session
  (`aktualen-li-blue-dollar.md`: after the 2025 liberalization the official/blue spread
  narrowed to a few percent, so banks/authorized exchange houses are now the safe default).
  Rewrote the answer to match — this is exactly the "contradiction between pages" failure mode
  the task brief calls out by name.
- **`src/data/immigration-content.ts`, `immigration-hub-content.ts`,
  `immigration-topic-content.ts`, `guide-pillar-ekonomika.ts`, `guide-pillar-bezopasnost.ts`** —
  these already carry an explicit "Проверено \<date\>" claim-level provenance pattern (good
  practice, already in place before this session). Re-verified the specific underlying pages
  live (DNM tourist-entry docs, residencias FAQ, Decreto 366/2025, Ley 25.871, Disposición
  758/2022, INDEC's IPC portal, BCRA's exterior-and-exchange page, ARCA's tax-free page, the
  migrant-account page, Buenos Aires's safety/health page) and bumped only the specific stamps
  whose cited source I could independently confirm — left others at their original date where a
  fetch timed out.
- **Lesson learned the hard way**: an initial attempt to bump the "Проверено 17.07.2026" stamp
  in `blog-argentina-tourist-visa-2026.ts` broke two vitest contracts
  (`blog-start-here-legacy-manual.test.ts`, `blog-resolver-public.test.ts`) that pin this exact
  post's `dateModified` field against its in-body "Проверено" stamp. That block cites *three*
  sources and I could only re-confirm two of them (the DNM `migraciones/turistas` page timed
  out) — the pinning test correctly caught that bumping the stamp without updating
  `dateModified` would have silently claimed a full re-verification that hadn't actually
  happened. Reverted the stamp to its original date rather than treating "close enough" as
  "verified". **Takeaway for future passes:** before bumping any "Проверено"/`checked_at`
  stamp, grep for it in `*.test.ts` first — some of these dates are contract-pinned on purpose.
- **Confirmed already correct, no change needed**:
  - `src/data/blog.ts`'s `quarantinedLegacyManualSlugs` mechanism already excludes
    `blue-dollar-argentina-2026` from public indexing with an explicit reason ("doesn't safely
    reflect the BCRA regime after 2025 changes") — a prior agent already found and fail-closed
    this exact class of problem.
  - `src/types/place.ts`'s `PLACE_CATEGORY_LABELS` is a `Record<PlaceCategory, string>` (all 15
    categories, including `city`/`town`/`viewpoint`/`national_park`) — TypeScript's
    exhaustiveness makes it impossible to ship a place category without a Russian label, so the
    long-standing "raw enum leaks to `/places`" concern from the old master-goal doc is already
    structurally closed.
  - `src/data/testimonials.ts` is an explicitly-emptied array with a comment recording that fake
    homepage quotes were already removed by a prior agent ("Legacy static homepage quotes
    removed — homepage loads reviews from DB or tour seed").
  - `src/data/public-organizers.ts` has exactly one real identity (the site owner) — no demo/
    fake organizer profiles.
  - `src/data/legal-content.ts` (privacy/terms/booking/refunds/cookies/affiliate pages) already
    distinguishes "заявка" (request) from confirmed booking/payment throughout, and correctly
    frames the platform as an intermediary rather than claiming capabilities (instant booking,
    guaranteed refunds) that aren't built — consistent with the task's commercial-honesty rule.
  - `npx tsx scripts/public-editorial-audit.ts` — 115 navigation links, 0 errors, both before
    and after this session's edits.
- **P1 media-rights backlog** (11 hero images below the 1200px gate; 2 unmanaged Wikimedia
  assets with no rescuable source URL) — investigated in the previous pass (§4 P1 list above,
  unchanged); still open, needs either a higher-resolution re-source per image or a
  reverse-image-search capability this session doesn't have. Not attempted again this session
  since nothing new was learned that would change the recommended action.

## 4c. Remaining P1 (carried over, unchanged)

**P1 — media rights/quality gaps** (from the pre-existing `docs/content-overhaul/issue-ledger.csv`,
still open, evidence command `npm run media:rights:audit`):
- 11 hero images below the 1200px-width publication gate (Aconquija, Los Arrayanes, Trevelín,
  Villa La Angostura, Valdés-peninsula blog hero, Bariloche cover ×2, Cueva de las Manos,
  Purmamarca gallery-1, Tren de las Nubes, Bariloche Lakes tour hero) — all already have valid
  rights metadata, just need a higher-resolution replacement sourced under the same or a
  compatible free license. Do not upscale existing files (that would misrepresent a
  lower-resolution photo as higher fidelity); source a genuinely larger derivative or an
  alternate free-licensed photo of the same place.
- `media/places/el-chalten/hero.jpg` and `media/places/purmamarca/hero.jpg`: **investigated,
  not fixed.** `git log --diff-filter=A` shows both were added in the same commit as their
  already-attributed `gallery-1.jpg` siblings ("Add local Wikimedia media library…"), and that
  commit message states the whole batch is Wikimedia-sourced — but the manifest entry for the
  `hero.jpg` file specifically was never written, and the file is not byte-identical to
  `gallery-1.jpg` (different MD5, different dimensions), so its exact Wikimedia source URL
  cannot be honestly reconstructed without a reverse-image search capability this session
  doesn't have. Fabricating a plausible-looking `sourceUrl` would violate the "never invent a
  source" rule, so left it as a clearly-diagnosed open item rather than a guessed fix.
- `search:meilisearch-production`: external/blocked — production Meilisearch host/key
  configuration can't be verified from this sandbox; needs the actual production environment.

**P3/P4 — editorial polish** (from `editorial-audit.ts`, non-blocking):
`best-time-to-visit-argentina`, `argentinian-steak-guide` (×3 sections),
`tango-beginners-guide` (long text + 9 callouts in one section — consider trimming),
`patagonia-packing-list`, `argentina-2-nedeli-marshrut`, `vnzh-argentina-rezidenciya` each have
one section flagged "long text without a visual block". None are factually wrong; they're
candidates for adding a table/diagram/callout on a future editorial pass, not urgent.

## 5. What was deliberately *not* done, and why

- Did not run a full production build + live-server sitemap crawl (`CONTENT_OVERHAUL_BASE_URL`)
  in this pass — that needs a stable local/staging candidate and is explicitly called out in
  `docs/content-overhaul/README.md` as a separate step; the KB-level fixes above don't depend
  on it, and forcing a full build/boot cycle for every content edit would have cost far more
  time than the fact-checking itself for comparatively little additional signal (the existing
  `content:links --strict` already gives 0-broken-public-link evidence without it).
- Did not touch `docs/project-governance/*` (Sprints 1–6, Supabase/Vercel infra program) — out
  of scope for a content task, and actively marked as a separate ongoing program by its own
  governance rules.
- Did not blindly bump `checked_at`/`last_verified` dates on any source without independently
  re-fetching it this session. Every date change in the two commits above is backed by an
  actual `WebFetch`/`WebSearch` call recorded in this log.
- Did not delete or rewrite `content/knowledge-base/README.md`'s outdated "sync not started"
  claim — flagged here instead, since rewriting internal process docs wasn't the highest-value
  use of remaining time versus fixing more live user-facing content.

## 6. Verification trail (cumulative, current state)

- `npx tsc --noEmit` — clean, rerun after every commit in this log.
- `npx eslint` on all changed `.ts`/`.tsx` — clean.
- `npm run audit:quick` — 488 test files / 2391 tests passed, 0 failed; typecheck, lint,
  fresh KB manifest rebuild, and 31/31 release-evidence contracts all green. Rerun after every
  commit; the one time it caught a real regression (the "Проверено" date-pinning issue, §4b) it
  was fixed by reverting the over-eager edit, not by loosening the test.
- `python3 content/knowledge-base/_index/build_manifest.py` — 0 structural/manifest errors,
  0 duplicate ids, 0 broken `related`/wikilinks. `kbPublic` 219 → **237/691** by the end of this
  session (started this session's freshness sweep at 243 before it was mechanically hit by the
  time-driven drift described in §3; 6 entries remain quarantined pending the WAF/timeout-blocked
  sources in §4).
- `npx tsx scripts/content-overhaul-inventory.ts` — `kbQualityBlocked` (outside the intentional
  archive) **24 → 6**; `requiredTopicGaps` 0/38 throughout.
- `npm run content:links --strict` — `brokenPublic: 0` / 909 unique external URLs (one fewer
  than the original 910 after removing the dead citizenship-service link from 4 files).
- `npx tsx scripts/public-editorial-audit.ts` — 115 navigation links, 0 errors.
- `npx tsx scripts/editorial-audit.ts` — 290 posts, 0 errors, 9 warnings (down from 24; the
  remaining 9 are non-blocking editorial-polish suggestions, see §4c P3/P4).
- `npx tsx scripts/blog-editorial-readiness.ts --strict` / `guide-editorial-readiness.ts --strict`
  — 67/67 and 29/29, including all 9 sensitive blog posts.

## 7. Session log (commits, in order)

1. `fix(content): derive editorial widget allowlist from TravelWidgetRenderer`
2. `content: fact-check knowledge base sources (citizenship, banking, visa, tax)`
3. `content: close KB publication-gate regressions from freshness sweep`
4. `docs: add content overhaul progress log; refresh audit snapshots`
5. `content: continue KB fact-check sweep (safety, medicine, property, budget)`
6. `content: complete first pass of KB fact-check backlog (money, work, visas)`
7. `content: fix stale SMVM figure in featured VNZh blog post; refresh immigration hub verification dates`
8. `content: refresh verification dates for guide pillars after re-checking sources`
9. `content: fix stale blue-dollar advice in site FAQ; correct an over-eager date bump`

All nine are on `cursor/content-overhaul-e4a2`, pushed, PR #38 (draft).
