# P0/P1 root-cause matrix

| Hypothesis | Reproduction | Root cause status | Scope | Fix/evidence next |
|---|---|---|---|---|
| Supabase outage becomes empty catalog | production APIs return 200 empty while health is 503; local fault runtime now returns 503/LKG | code cause fixed in candidate: wrappers converted query/client errors to `[]/null`; REST infrastructure cause confirmed `exceed_egress_quota`; production direct-PG cause unresolved | catalog APIs, RSC, search/sitemap/recommendations, detail | deploy exact WP-001 candidate after Vercel/Supabase recovery; diagnose direct PG |
| Operational error becomes cacheable 404 | source trace shows detail wrappers collapse error to null; public resolver can emit missing | confirmed local root cause; live detail slug unavailable because catalog has no cards | tour/excursion detail, metadata, affiliate/booking consumers | propagate unavailable; prevent cache poison; crawl all cards in preview |
| CMS operational error becomes missing/empty or cached fallback | source/fault tests reproduce single/list/comment error→`null`/`[]`; exact quota runtime formerly returned empty comments; blog cache wrapped fallback for 300 s | fixed in WP-024 `d4fbbbc1`: typed read/resolution, fallback outside cache, CMS-only/comments retryable 503 and bounded class-only logging | blog/destination/place/guide/knowledge/legal/author CMS detail, catalog, slugs, comments and existence | 72 focused/related, 2 133 full, build 929/929, local recovery and exact preview `9nLoBa…` backend/browser proof; healthy recovery and production promotion remain |
| External partner message crosses logging boundary | exact WP-024 builds reproduced raw Tripster quota text; source trace found shared logger `result.message`, Tripster/YouTravel thrown errors and External Orders `error.details` | fixed in WP-026 `966be464` candidate: allowlisted source/class/retryability log and safe compatibility code; production remains legacy | build/runtime observability and incident evidence | 33 focused, 2 135 full, 929/929 build and real quota build/runtime scan pass; exact preview/production proof blocked by Vercel |
| Dev dependency high findings can be removed by a safe override or Next/ESLint major upgrade | production audit is 0; full audit is exact nine-package advisory 1124334 graph. Patched `brace-expansion@5.0.8` changes the CommonJS export expected by minimatch 3; latest ESLint plugins still retain minimatch-3 branches even with Next 16/ESLint 10 | disproved as a safe immediate remediation; confirmed upstream compatibility constraint | developer/CI supply chain only; no production dependency path | WP-025 fail-closed exception through 2026-08-12; remove on compatible upstream, never force/extend without new evidence |
| Recovery smoke accepts an error route as a commercial detail | exact `a3301ec9` local recovery run selected `/tours/error-*` and `/excursions/error-*` and exited 0 | fixed in WP-020: whole-document path regex matched Next chunk filenames; href-bound discovery now rejects assets, reserved/error/page routes | production smoke, preview promotion gate, catalog/detail readiness claims | 6/6 focused, 31/31 evidence, exact local real-link recovery smoke; exact broken preview returns null; production same-artifact proof after P0 recovery |
| Parallel catalog/detail rendering exhausts DB/provider capacity | exact `ed29b335` 5-worker browser run produced partner 429/timeouts and direct-PG `too many clients`/reserved slots; 1-worker rerun 17/17 | confirmed code fan-out root cause: two public-detail filters use unbounded `Promise.all`; Tripster/YouTravel fallback opens standalone `pg.Client` per detail | tours/excursions, optional embeds, SSR latency, unrelated requests sharing DB/provider capacity | WP-021 bounded concurrency + in-flight dedupe/shared bounded PG primitive; deterministic fault/load contract; exact parallel preview QA |
| Catalog/detail/snapshot mismatch | historical multiple REST/PG/partner/cutover paths; current health blocks live reconciliation | partially confirmed architectural cause | catalog, detail, slugs, sitemap, counts | unify result contract, then target reconciliation and count parity |
| Genuine commercial detail exceeds recovery latency budget | two consecutive default 15-second recovery smokes reach all public pages then abort entering commercial detail; direct genuine tour/excursion return 200 in 6.5s/9.7s, `/excursions` catalog measured 14.986s, and 60-second diagnostic smoke passes | confirmed latency gap; root stage not yet isolated. Missing/error-route false positive is excluded, and WP-026 changes only logging/error text | `/tours`, `/excursions`, genuine detail fallback and release smoke | WP-027 per-stage platform/partner/PG timing, remove redundant degraded fallback work, require unchanged 15-second smoke pass; do not relax default timeout |
| `/excursions` SSR loader/empty | production hydrated UI says no results; local fault browser now shows actionable route error at desktop/mobile | source failure semantics fixed locally; streaming/no-JS behavior still requires preview | SSR/no-JS/slow network | immutable preview no-JS/slow 3G check |
| Product promises exceed capability | production footer/marketplace/guide copy makes blanket marketplace, verification, review and payment claims | confirmed copy/control mismatch; high-risk sources fixed in WP-002 candidate and protected by locale/source tests | global footer/hero/about/navigation, marketplace home, booking/safety guides | redeploy exact `ef447d8e`; remote browser + legal/capability follow-up |
| Exact candidate cannot reach preview | repeated candidates are initially rejected with `Account is blocked`; latest `84f6244b` recovered after about seven minutes as `CJ3fcfursTMefpDtXoJRX7h1TpmN` | external Vercel account/build state is intermittent and project/runtime-log read scope remains unavailable | preview/release evidence and direct-PG diagnosis | retain SHA ↔ deployment ↔ status timeline; restore stable account and read-only logs/scope before promotion |
| Migration state differs | baseline 102, local/build metadata 107, DB unreadable | evidence gap, not proven drift | all DB-backed modules | canonical journal checksum query after access restoration |
| Recovery is reliable | runbook exists, restore rehearsal not_run | confirmed governance gap | entire asset | configure encrypted backup + disposable restore proof |
| Editorial guides are slow during catalog outage | safety/yazyk triggered full marketplace fetch and N detail resolution despite having no tour widget | fixed in candidate `b53daadd`: unconditional page-level catalog dependency; schema predicate now skips it | all `/guide/[slug]` pillars without `tour-embed` | remote preview benchmark after Vercel recovery |
| Optional weather tour widget delays parent guide | weather schema legitimately has `tour-embed`; route awaited catalog data, and the public-detail filter then collapsed unavailable candidates into `[]` | fixed and remote-proven in `189684fa` / `NnmUYR…`: local Suspense boundary plus strict optional-widget resolution | `/guide/pogoda-i-sezonnost` and future tour widgets | production proof after P0 recovery and same-artifact promotion |
| Current architecture/interaction inventory is incomplete | historical release CSV records 129 pages/234 handlers/58 curated interactions; source has 157 pages/312 handlers and substantially broader UI mutation surface | confirmed governance root cause: manually curated snapshot had no deterministic generator or stale check | release scope, journey coverage, ownership and prioritization | fixed in WP-005: generated 471 routes/470 data rows/2 298 interactions; WP-006 adds effect/test evidence for critical mutations |
| Critical mutation evidence is indistinguishable | all 2 298 generated interactions were `source_only`; template payment URLs were recorded as `dynamic` despite existing contracts | fixed in WP-006: one blanket static label represented a traceability gap, not a tested/untested verdict; URL templates lacked normalized parameters | booking, payments, refunds, payouts, shop, privacy, organizer application | generated 11-journey manifest: 8 unit-contract linked, 3 privacy source-only, all production effects unknown |
| Admin privacy transition may race and lose durable audit | route read current request, updated by ID, allowed reject from processing and mutated profile before processor claim | route cause fixed in WP-007: status CAS, active-state guard, request-row actor metadata and processor-only profile mutation; live DB unproven | privacy approval/rejection, profile soft-delete, compliance audit | exact build/route tests; transactional audit design only after migration parity |
| Privacy deletion retry is consistent | request creation is read→insert; processor performs many calls; profile is anonymized before email-linked cleanup; completion email was inside the destructive catch and failure marking matched only ID | WP-008 fixed retry identity loss; WP-009 fixes notification-driven completed→failed regression and adds `processing` CAS to terminal writes; remaining lease/checkpoint/DB gap is confirmed but not destructively reproduced | duplicate requests, partial anonymization, notification, retry after failed state | DB uniqueness/lease/atomic audit design after canonical parity; no automatic failed retry before then |
| Payment checkout is single-provider and monotonic | parallel Stripe/Mercado routes read the same booking, called different providers, then blind-wrote one snapshot; request origin was passed into token-bearing callbacks | fixed in WP-010 candidate: booking-version CAS claims one provider before external call, result write CAS protects webhook/admin state, callback origin comes from canonical site boundary | payment-link checkout, webhook races, double-charge/trust boundary | live provider/DB replay only after healthy disposable/canonical environment; retain fake concurrency route tests |
| Payment capability response is bounded and safe | public status route returned the full booking and raw storage configuration error under local production smoke | fixed in WP-010 candidate: explicit checkout projection and public-safe 503; passports/phone/CRM/owner/private metadata exclusion is route-tested | `/api/bookings/payment-link/[token]`, payment/result pages | remote invalid-token/browser proof; live valid-token projection only after controlled data environment |
| Exact local build and preview use the same artifact | Vercel env set CI behavior, build wrote `.next`, local start defaulted to stale `.next-production`; health SHA alone came from runtime env and did not reveal stale route code | confirmed release-evidence root cause; first result rejected and final exact SHA rebuilt with explicit `NEXT_DIST_DIR=.next-production` | local production-equivalent QA and SHA claims | require exact distDir plus behavioral assertion in every packet |
| Price quote consumes a real availability slot | quote modal sets `priceQuoteRequest: true`, but `/api/bookings` unconditionally bootstrapped the selected slot and passed it to the atomic RPC; the RPC incremented `booked_count` | fixed in WP-011 candidate: reservation slot is derived server-side only for real scheduled booking intent | booking quote lead, availability capacity, organizer inventory | route integration proves quote persistence with zero reservation; live RPC proof after data-plane recovery |
| Scheduled booking can persist without confirmed inventory | `ensureAvailabilitySlotForBooking` returned false on lookup/bootstrap failure, the route ignored that result, and the RPC tolerated a requested slot row being absent | fixed in WP-011 candidate: required canonical slot fails closed with 409 before persistence/notification | native booking create, oversell prevention, notification | fake-store route test proves zero side effects; reconcile deployed RPC/migration before live proof |
| Booking dependency error exposes internal detail | unknown atomic RPC messages flowed through the storage wrapper and route response | fixed in WP-011 candidate: unknown errors throw to telemetry and become generic public 503; known command conflicts remain explicit | public `/api/bookings`, SQL/config confidentiality | injected secret-marker route test; exact preview artifact/recovery UI proven, but no remote POST mutation was executed |
| Signed webhook can acknowledge payment without a financial ledger | Stripe and Mercado routes applied booking patch first; `persistWebhookChargeTransaction` caught every ledger error; route returned 200 and the processed event ID made retry a non-repairing boolean false | code root cause fixed in WP-012 `77cf5674`: detailed outcome, ledger-required 2xx, retryable 500, exact replay repair and insert-first unique recovery; live DB/provider effect unproven | booking paid/refunded state, `payment_transactions`, receipts, refund/reconciliation | signed route/concurrency tests pass; after data-plane recovery verify canonical index/RLS and controlled provider replay; commission/outbox remains separate |
| Mercado payment lifecycle events collapse to one replay identity | absent notification ID triggered fallback `mp-payment-${payment.id}`, reused by pending and paid updates for one resource | code root cause fixed in WP-012: notification identity is mandatory before provider/DB, while `data.id` remains charge identity | Mercado state progression and webhook replay classification | distinct signed notification tests pass; verify real provider payloads/logs only after controlled recovery |
| Charge row exists but commission/notification can be missing | commission snapshot lookup and notification occur after the durable charge and are deliberately best-effort; no transactional outbox/reconciliation evidence exists | confirmed residual architecture gap, not silently classified as webhook failure | organizer commission, customer message, operations reconciliation | WP-013+ planning: expose reconciliation/outbox design after live journal parity; keep charge 2xx invariant independent |
| Refund request assumes booking USD/provider snapshot instead of captured money | tourist, organizer and admin routes passed `paidAmountUsd` plus hard-coded `USD` and derived provider from booking, while the atomic RPC requires the source charge fields | fixed in WP-013 `26aeda4c`: service resolves the completed charge and owns provider/amount/currency; admin mismatch fails before RPC | non-USD refund request, cumulative reservation, ledger display | ARS/replay route+service tests pass; canonical RPC/RLS/live provider effect pending data-plane recovery |
| Refund mutation grants broader email access than refund read | POST accepted any session whose email matched contact email; GET/shared booking policy required `booking.userId` or organizer role/ownership | fixed in WP-013: POST uses shared `canAccessBooking`; matching email alone is 403 | tourist refund ownership and guest attachment | authenticated App Router owner/organizer/email-only matrix; live RLS pending |
| Provider uncertainty leaves refund stuck processing | atomic claim transitions pending→processing before external call; provider/finalize failures cannot re-enter approve and no reconcile/lease exists | confirmed residual; automatic retry rejected until provider lookup and atomic recovery ownership are designed | admin refund approval, provider accounting, customer balance | register R-024; design after live journal parity and controlled provider sandbox evidence |
| Booking idempotency replay exposes a current CRM row across actors | booking ID depends only on the client operation key; request fingerprint excludes actor; service-role RPC compares only fingerprint and returns `to_jsonb(v_existing)`; route forwarded it verbatim | fixed at the only application call site in WP-014: stored/input owner must match, authenticated retry first runs confirmed-email guest attach, and both create/replay return only `{booking:{id}}`; live RPC and DB-layer predicate remain unproven | native booking create, guest→account claim, travelers/payment/CRM privacy | 21 focused actor/response tests, 2 070 full tests and exact local build; add RPC predicate only after 107-file journal parity, then run controlled live replay |
