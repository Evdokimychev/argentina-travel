/**
 * Сбор контактных данных (имя, email, телефон, сообщение гиду) в формах
 * бронирования партнёрских экскурсий и туров временно отключён.
 *
 * Причина: для анонимных пользователей контакты всё равно не передаются
 * партнёру (Tripster / YouTravel) — форма вводила в заблуждение и давала
 * лишние ошибки валидации. Бронирование теперь открывает сайт партнёра
 * с датой, временем и числом туристов; контакты турист заполняет уже на
 * стороне партнёра.
 *
 * Код формы сохранён (заархивирован) внутри компонентов и закрыт этим флагом.
 * Чтобы вернуть сбор контактов — установите ENABLE_PARTNER_CONTACT_FORM = true.
 *
 * Тип намеренно объявлен как boolean (а не литерал false), чтобы оба ветвления
 * оставались валидными и не считались недостижимым кодом.
 */
export const ENABLE_PARTNER_CONTACT_FORM: boolean = false;
