/**
 * Supabase client placeholder.
 *
 * When connecting to Supabase, install @supabase/supabase-js and implement:
 *
 *   import { createClient } from '@supabase/supabase-js';
 *   export const supabase = createClient(
 *     process.env.NEXT_PUBLIC_SUPABASE_URL!,
 *     process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
 *   );
 *
 * Then update fetchTourDetail() in src/lib/tours.ts to query:
 *   tours, tour_gallery, tour_places, tour_itinerary_days,
 *   tour_reviews, tour_accommodations, tour_dates, organizers
 */

export {};
