import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Совместные поездки — личный кабинет",
};

export default function ProfileGroupTripsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
