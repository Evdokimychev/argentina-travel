import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Заявка — личный кабинет",
};

export default function ProfileBookingDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
